<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">


    <Style x:Key="LeftRightPosition" TargetType="RadioButton">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Grid>
                        <!-- Изображение для невыбранного состояния -->
                        <Image x:Name="StateImage" Source="/Views/Resources/Position/leftRight.png" Width="70" Height="70"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="StateImage" Property="Source" Value="/Views/Resources/Position/leftRightMouseOver.png"/>
                        </Trigger>

                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="StateImage" Property="Source" Value="/Views/Resources/Position/leftRightChecked.png"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="RightLeftPosition" TargetType="RadioButton">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Grid>
                        <!-- Изображение для невыбранного состояния -->
                        <Image x:Name="StateImage" Source="/Views/Resources/Position/rightLeft.png" Width="70" Height="70"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="StateImage" Property="Source" Value="/Views/Resources/Position/rightLeftMouseOver.png"/>
                        </Trigger>

                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="StateImage" Property="Source" Value="/Views/Resources/Position/rightLeftChecked.png"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="CenterLeftPosition" TargetType="RadioButton">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Grid>
                        <!-- Изображение для невыбранного состояния -->
                        <Image x:Name="StateImage" Source="/Views/Resources/Position/centerLeft.png" Width="70" Height="70"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="StateImage" Property="Source" Value="/Views/Resources/Position/centerLeftMouseOver.png"/>
                        </Trigger>

                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="StateImage" Property="Source" Value="/Views/Resources/Position/centerLeftChecked.png"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="CenterRightPosition" TargetType="RadioButton">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Grid>
                        <!-- Изображение для невыбранного состояния -->
                        <Image x:Name="StateImage" Source="/Views/Resources/Position/centerRight.png" Width="70" Height="70"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="StateImage" Property="Source" Value="/Views/Resources/Position/centerRightMouseOver.png"/>
                        </Trigger>

                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="StateImage" Property="Source" Value="/Views/Resources/Position/centerRightChecked.png"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>