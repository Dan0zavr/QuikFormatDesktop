<UserControl x:Class="QuikFormatDesktop.View.Pages.NumberedNumberingCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuikFormatDesktop.View.Pages"
             xmlns:converters="clr-namespace:QuikFormatDesktop.View.Converters"
             mc:Ignorable="d">

    <UserControl.Resources>
        <converters:UnicodeToCharConverter x:Key="UnicodeToCharConverter"/>
    </UserControl.Resources>

    <GroupBox Header="Стиль нумерованного списка" FontSize="14" Padding="4" Background="{StaticResource MainBackground}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition/>
                <RowDefinition Height="auto"/>
                <RowDefinition/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0">
                <TextBlock Text="Имя стиля:" FontSize="12" Margin="0,0,0,5"/>
                <TextBox x:Name="NumberingStyleName" FontSize="12" Margin="0,0,0,15" Text="{Binding NumberingStyleName}"/>
            </StackPanel>

            <Grid Grid.Row="2">

                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Параметры" FontSize="12" Margin="0,0,5,5"/>
                    <Border Grid.Column="1" Height="1" Background="{StaticResource InnerBorderColor}" VerticalAlignment="Center"/>
                </Grid>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Margin="5, 0, 0, 0"  Text="Маркер:" FontSize="12"/>
                        <ComboBox Margin="5,0,0,10" 
                            ItemsSource="{Binding Markers}"
                            SelectedItem="{Binding SelectedMarker}"
                            SelectedValue="{x:Null}">

                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock/>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </StackPanel>
                </Grid>
            </Grid>

            <Grid Grid.Row="4" Margin="0,20,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Text="{Binding PStatusMessage}" 
                           Foreground="Green" 
                           FontWeight="Bold"
                           VerticalAlignment="Center"
                           Margin="0,0,10,0" />

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="Сброс" 
                        Width="100" 
                        Margin="0,0,10,0" 
                        Command="{Binding FormulaDeleteCommand}"/>
                    <Button Content="Сохранить" 
                        Width="100" 
                        Command="{Binding AddFormulaCommand}"/>
                </StackPanel>
            </Grid>
        </Grid>
    </GroupBox>
</UserControl>
