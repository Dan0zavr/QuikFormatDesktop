<UserControl x:Class="QuikFormatDesktop.View.Pages.TextStylePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuikFormatDesktop.View.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="650">
    <Grid Background="{StaticResource MainBackground}">
        <Grid Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <local:TextCard Grid.Column="0" MaxHeight="600" MaxWidth="500"/>

            <local:ParagraphCard Grid.Column="2" MaxHeight="600" MaxWidth="500"/>
        </Grid>
    </Grid>
</UserControl>
